spring:
  application:
    name: wealthledger-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/wealthledger
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
  jpa:
    open-in-view: false	
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc.lob.non_contextual_creation: true
        default_schema: public

logging:
  level:
    root: INFO	
cors:
  allowed-origin: ${FRONTEND_URL}
jwt:
  secret: ${JWT_SECRET}
  expirationMs : 86400000
mf:
  save-nav-schedule-cron: "0 0 */6 * * *"
  nav-file-download-url: "*****"
  nav-file-path: "*****"
  nav-staging-batch-size: 100
stock:
  save-ltp-schedule-cron: "0 0 */6 * * *"
  ltp-file-download-url: "*****"
  ltp-file-path: "*****"
  ltp-staging-batch-size: 100